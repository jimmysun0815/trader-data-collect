# API
requests>=2.28.0

# Polymarket CLOB（如需）
py-clob-client>=0.1.0

# CEX score + 回测（cex_scorer_v4, run_backtest, cex_scorer_backtest）
numpy>=1.21.0
pandas>=1.3.0
scikit-learn>=1.0.0
scipy>=1.7.0

# 可选：Binance OHLCV（cex_scorer_v4）
ccxt>=4.0.0

# 可选：模型持久化（joblib，若用 zeff 模型可保留）
joblib>=1.1.0
